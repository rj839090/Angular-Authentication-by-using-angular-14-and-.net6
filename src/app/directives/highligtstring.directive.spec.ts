import { HighligtstringDirective } from './highligtstring.directive';

describe('HighligtstringDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligtstringDirective();
    expect(directive).toBeTruthy();
  });
});
